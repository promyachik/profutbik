User-agent: *
Allow: /

{{/* PF531I: запреты убраны, потому что запрещать стало нечего.

     Здесь стояли две строки:

         Disallow: /promyachik/categories/
         Disallow: /promyachik/tags/

     Путь /promyachik/ остался от прежнего адреса сайта и давно отдаёт 404,
     так что эти строки не закрывали ничего, а настоящие /categories/ и
     /tags/ спокойно попадали в обход и в sitemap. Теперь разделы отключены
     в hugo.toml и не создаются вовсе — это надёжнее запрета.

     Ничего другого закрывать не нужно: черновиков в сборке нет, служебных
     разделов тоже. Пустой запрет хуже отсутствующего — он выглядит как
     работающая защита. */}}
Sitemap: {{ "sitemap.xml" | absURL }}

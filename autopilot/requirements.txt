# Зависимости движка 3.4. Всё остальное в конвейере — стандартная библиотека.
#
# pillow  — сборка портретов и логотипов.
# rembg   — вырезание фона у портрета на шаге 3B. Тянет за собой onnxruntime
#           и модель u2net.onnx (176 МБ), которую качает при первом вызове.
#           Модель кэшируется в ~/.u2net средствами workflow, иначе она
#           скачивалась бы каждые полчаса.
pillow>=10.0
rembg==2.0.76
onnxruntime==1.27.0
